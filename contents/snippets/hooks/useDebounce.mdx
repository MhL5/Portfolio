## Installation

<InstallationTabs name="useDebounce" />

## Usage

```tsx
import { useDebounce, useDebounceValue } from "@/hooks/useDebounce";
```

<br />

```tsx
// useDebounce - executes a function after delay
useDebounce(
  () => {
    // This will run after 500ms of inactivity
    console.log("Debounced function executed");
  },
  500,
  [dependency],
);

// useDebounceValue - debounces a value
const [search, setSearch] = useState("");
const debouncedSearch = useDebounceValue(search, 300);
```
